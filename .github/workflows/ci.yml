name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test --all-targets

  linux-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl
      - run: sudo apt-get update && sudo apt-get install --yes musl-tools
      - run: cargo build --release --target x86_64-unknown-linux-musl
      - run: docker build --tag netsandbox-e2e tests/e2e
      - run: >-
          docker run --rm --privileged
          --volume "$PWD/target/x86_64-unknown-linux-musl/release/netsandbox:/opt/netsandbox:ro"
          netsandbox-e2e
          /usr/local/bin/linux-host-scenario
      - uses: actions/upload-artifact@v7
        with:
          name: netsandbox-linux-guest-amd64
          path: target/x86_64-unknown-linux-musl/release/netsandbox

  macos-test:
    needs: linux-e2e
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: actions/download-artifact@v8
        with:
          name: netsandbox-linux-guest-amd64
          path: artifacts
      - run: chmod 755 artifacts/netsandbox
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test --all-targets
      - run: >-
          NETSANDBOX_EMBED_LINUX_GUEST="$PWD/artifacts/netsandbox"
          cargo build --release --locked
      - run: target/release/netsandbox --version
